# Requirements for hpl_np.py — the NumPy/LAPACK HPL Linpack benchmark.
#
# Install with:
#     pip install -r requirements.txt
#
# Only numpy is needed; pip wheels bundle threaded OpenBLAS.

numpy>=1.24
